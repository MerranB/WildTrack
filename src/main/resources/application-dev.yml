spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}

  jpa:
    show-sql: true

logging:
  level:
    com.wildtrack: DEBUG
    root: WARN
    org.hibernate.SQL: DEBUG