spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}

  jpa:
    show-sql: false

logging:
  level:
   com.wildtrack: INFO
   root: WARN

scheduler:
  cronTime:
    updateDatabase: "0 30 21 * * ?"
    geoFence: "0 30 23 * * ?"
  enabled: false